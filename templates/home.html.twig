{% extends 'base.html.twig' %}
{%block body %}
<h1 class="text-center">Genres</h1>
    <div class="container-xl  ">
    <div class="row">
        {% for g in genre %}
            <div class="col">
                <div class="card border border-light">
                    <div class="card-body justify-content-center">
                        <p class="fw-bold text-light btn ">
                            <a href="{{ path('movie',{id: g.id}) }}"> {{ g.name}} </a>                                </p>
                    </div>
                </div>
            </div>



        {% endfor %}
        <a class="btn btn-primary" href="home"><b>back</b></a>
    </div>


{%endblock %}